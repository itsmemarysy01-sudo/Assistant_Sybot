{
  "name": "teammarysy-bot",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "check": "find . -maxdepth 1 -name \"*.js\" -print0 | xargs -0 -n1 node --check"
  },
  "devDependencies": {
    "wrangler": "^3.80.0"
  }
}
